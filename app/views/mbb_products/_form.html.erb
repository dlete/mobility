<%= form_for(@mbb_product) do |f| %>
  <% if @mbb_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mbb_product.errors.count, "error") %> prohibited this mbb_product from being saved:</h2>

      <ul>
      <% @mbb_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
