<%= form_for(@mbb_channel) do |f| %>
  <% if @mbb_channel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mbb_channel.errors.count, "error") %> prohibited this mbb_channel from being saved:</h2>

      <ul>
      <% @mbb_channel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
