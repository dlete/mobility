<h1>Listing institution_categories</h1>

<table>
  <tr>
    <th>Category</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @institution_categories.each do |institution_category| %>
  <tr>
    <td><%= institution_category.category %></td>
    <td><%= link_to 'Show', institution_category %></td>
    <td><%= link_to 'Edit', edit_institution_category_path(institution_category) %></td>
    <td><%= link_to 'Destroy', institution_category, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Institution category', new_institution_category_path %>
