<%= form_for(@institution_population) do |f| %>
  <% if @institution_population.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@institution_population.errors.count, "error") %> prohibited this institution_population from being saved:</h2>

      <ul>
      <% @institution_population.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :academic_year %><br />
    <%= f.number_field :academic_year %>
  </div>
  <div class="field">
    <%= f.label :institution %><br />
    <%= f.collection_select :institution_id, @institutions,
                            :id, :name,
                            :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :staff %><br />
    <%= f.number_field :staff %>
  </div>
  <div class="field">
    <%= f.label :enrollments %><br />
    <%= f.number_field :enrollments %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
