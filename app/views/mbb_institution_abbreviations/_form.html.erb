<%= form_for(@mbb_institution_abbreviation) do |f| %>
  <% if @mbb_institution_abbreviation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mbb_institution_abbreviation.errors.count, "error") %> prohibited this mbb_institution_abbreviation from being saved:</h2>

      <ul>
      <% @mbb_institution_abbreviation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :institution_id %><br />
    <%= f.collection_select :institution_id, @institutions,
                            :id, :name,
                            :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :abbreviation %><br />
    <%= f.text_field :abbreviation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
